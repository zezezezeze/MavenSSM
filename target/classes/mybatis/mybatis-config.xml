<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<typeAliases>
		<package name="com.millery.mapper"/>
	</typeAliases>
	
	<!-- 配置mapper
	由于使用spring和mybatis的整合包进行mapper扫描，这里不需要配置了。
	必须遵循：mapper.xml和mapper.java文件同名且在一个目录 
	 -->
	<!-- mapper代理方式，批量加载mapper接口，只需制定mapper接口的包名，
	需要遵循mapper接口类名和mapper.xml映射文件名称一致，并且在同一个目录 -->
	<!-- <mappers>
		<package name="com.center.mapper"/>
	</mappers> -->
</configuration>